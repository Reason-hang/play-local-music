# 本地听歌 MP4 + AAC MVP 交付说明

> 状态：APK 已生成
> 更新时间：2026-08-06

## 目录

- [交付文件](#交付文件)
- [构建事实](#构建事实)
- [已完成与未覆盖](#已完成与未覆盖)

## 交付文件

- APK：`../../../../outputs/本地听歌-MVP-v1.1.2.apk`
- 源码：本仓库根目录 `work/Gramophone`（技术目录名保留以兼容上游工程）
- 包名：`org.akanework.gramophone`
- versionCode：`24`
- versionName：`1.1.2.19c8a81`
- 签名：Android 默认 debug 签名，仅用于本次安装试用；正式发布需替换为用户自己的 release keystore。
- SHA-256：`34ad2f24e98b88deb5d02d01a525ff3e4d0916b96f4b2db6d4016ec42fc941db`，见交付目录旁的 `本地听歌-MVP-v1.1.2.apk.sha256`。

## 构建事实

- `:app:assembleNonMinifiedRelease`：退出码 0，构建提交为 `19c8a81`。
- APK 是 Release 构建、未启用混淆，便于 MVP 排查问题。
- APK 静态清单无 `INTERNET`、`ACCESS_NETWORK_STATE` 和图片权限；包含用于读取本地 MP4 的 `READ_MEDIA_VIDEO`。
- `testDebugUnitTest`：新增 MP4 + AAC 规则 4/4 通过；全量 41/42 通过，1 个上游歌词解析断言失败。
- `apksigner verify --verbose`：v1、v2 签名验证通过；`zipalign -c -P 4 -v 4` 通过。
- `lintDebug`：未通过，9 个错误、96 个警告；错误均位于既有 `GramophonePlaybackService.kt` 和多语言资源文件，未报告本次 MP4 + AAC 改动文件。

## 已完成与未覆盖

已完成：本地扫描、播放器/队列/歌单/后台/媒体控制沿用上游；MP4 容器中的 AAC 音轨会被扫描并以音频方式加入歌曲列表、队列、搜索、删除和外部选曲；不显示 MP4 画面。可见界面和关键文案完成简体中文化；歌词、封面、均衡器和外链入口关闭；方案 B 离线权限处理完成。

未覆盖：没有连接红米 K80 Pro，未实测 MP4 + AAC 在 HyperOS 的后台、电量策略、锁屏、蓝牙和飞行模式；未清理底层网络/歌词/封面依赖；MP4 内非 AAC 音频编码及视频画面不作为本版验收承诺。
