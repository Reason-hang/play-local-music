# 构建与交付

> 状态：已构建；自动化验收部分通过
> 更新时间：2026-08-03

## 目录

- [环境要求](#环境要求)
- [构建命令](#构建命令)
- [交付文件](#交付文件)
- [失败处理](#失败处理)

## 环境要求

- JDK 21。
- Android SDK Platform 36.1（API 36 minor 1）与 Build Tools 36。
- 已初始化 `media3` 与 `hificore/src/main/cpp/libusb-cmake` 子模块。

## 构建命令

在仓库根目录执行：

```bash
JAVA_HOME=/path/to/jdk-21 \
ANDROID_HOME=/path/to/android-sdk \
./gradlew --no-configuration-cache --no-parallel :app:assembleNonMinifiedRelease
```

实际结果：退出码 0，生成 `app/build/outputs/apk/nonMinifiedRelease/本地听歌-1.1.1.eacd986-nonMinifiedRelease.apk`。该交付变体是 Release 构建但使用 Android 默认 debug 签名，因为未提供正式私钥；签名可验证且可安装，不等同于发布商正式签名。APK 应用标签已核验为“本地听歌”。

自动化任务：`testDebugUnitTest` 在增加 JDK 21 Robolectric 模块导出参数后为 37/38 通过，1 个既有歌词解析断言失败；`lintDebug` 报告 9 个错误、97 个警告，详见 `app/build/intermediates/lint_intermediate_text_report/debug/lintReportDebug/lint-results-debug.txt`。

## 交付文件

最终 APK 复制到交付目录并命名为 `本地听歌-MVP-v<version>.apk`，同时记录包名、versionCode、versionName、签名者和 SHA-256。源码目录为本文件所在仓库根目录。

## 失败处理

构建失败时先保留完整命令和首个根因，补充最小修复后重跑定向任务，再执行完整回归；不得用降低校验或隐藏错误的方式制造“通过”。
