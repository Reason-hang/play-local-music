# 验证记录

状态：1.6.1 交付；更新时间：2026-08-12。

## 自动化与静态证据

| 检查 | 命令或工具 | 结果 |
| --- | --- | --- |
| 视频续播定向测试 | `:app:testLocalDebugUnitTest --tests VideoResumeProgressStoreTest` | 3/3 通过：相同指纹恢复、文件修改后失效、播完清除、音频不建档。 |
| 课程变体定向测试 | `:app:testCourseDebugUnitTest --tests VideoResumeProgressStoreTest` | 3/3 通过。 |
| 全量双变体单元测试 | `:app:testLocalDebugUnitTest :app:testCourseDebugUnitTest` | 两个变体各 55 项、共 110 项通过，0 失败；`LrcUtilsTest` 各 34 项通过。 |
| 静态检查 | `:app:lintLocalDebug :app:lintCourseDebug` | 两个变体均完成，0 Error；各有 127 个既有 Warning，主要为上游多语言资源与 API 兼容提示。 |
| 双变体构建 | `:app:assembleLocalDebug :app:assembleCourseDebug` | 通过，分别生成可安装 APK。 |
| APK 结构 | `aapt2` | “本地听歌”：`org.akanework.gramophone.debug`；“多听课程”：`org.akanework.gramophone.course.debug`；均为 versionCode 33、versionName `1.6.1`、targetSdk 36。 |
| 离线边界 | 合并清单与 APK 清单检查 | 两个变体均不含 `INTERNET`、`ACCESS_NETWORK_STATE`。 |
| 签名与对齐 | `apksigner`、`zipalign` | 两包 v1/v2 签名有效、使用同一 SelfBuilt 证书，ZIP 对齐通过。 |
| 课程品牌资源 | `aapt2 dump resources` | 课程包的 `ic_launcher` 指向 course 覆写资源；含用户提供的 `course_launcher_mark`，启动页也覆写为同一标志。 |

## 红米 K80 Pro 真机边界

本轮执行 `adb devices -l` 时没有可用设备，因此未安装到设备，也不把本地构建当作真机验收。请在 Redmi 24122RKC7C 上按《产品范围与验收》的步骤，重点确认：两个图标并存、视频 A/B 互不覆盖进度、全屏进退、锁屏后台、系统回收后的最大 15 秒进度丢失边界。

## 交付校验值

| APK | SHA-256 |
| --- | --- |
| `outputs/本地听歌-1.6.1.apk` | `efb7186f1d4fd7c8ca72b7f4003f63ed89195cb4b02cb538149a98043966653d` |
| `outputs/多听课程-1.6.1.apk` | `3d14bc90bcae8e2ea0d2449206a7a32dfc087bcb778f311bef5e6763fead0e1a` |

签名为 SelfBuilt，不等同于正式商店签名；未提供正式 keystore 前，不能证明未来商店包与当前 APK 的升级兼容性。
