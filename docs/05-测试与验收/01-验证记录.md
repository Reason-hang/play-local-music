# 验证记录

> 状态：1.3.3 SelfBuilt APK 已构建、签名与清单核验；红米 K80 Pro 新包真机回归未执行
> 更新时间：2026-08-08

## 目录

- [当前证据](#当前证据)
- [未闭环验收](#未闭环验收)
- [验收判定规则](#验收判定规则)

## 当前证据

| 日期 | 检查 | 结果 |
|---|---|---|
| 2026-08-08 | `:app:assembleDebug` | 通过；生成 `本地听歌-1.3.3.7e1e6b9-debug.apk`，`versionCode 27`，`versionName 1.3.3.7e1e6b9`。 |
| 2026-08-08 | 定向单元测试 | 通过：`DiagnosticStoreTest` 1 项、`Mp4AacSupportTest` 5 项、`PlaybackSpeedPresetTest` 1 项、`BehaviorSettingsActivityTest` 1 项，共 8 项。 |
| 2026-08-08 | `:app:lintDebug` | 通过：0 个 Error、124 个既有 Warning；报告位于 `app/build/reports/lint-results-debug.html`。未用 baseline 隐藏问题。 |
| 2026-08-08 | `:app:assembleNonMinifiedRelease` | 通过。由于未提供正式 release keystore，产物仍不是可对外分发的正式签名版本。 |
| 2026-08-08 | APK 权限与入口检查 | `aapt2`：`targetSdk 36`；含 `READ_MEDIA_AUDIO`、`READ_MEDIA_VIDEO`；不含 `INTERNET`、`ACCESS_NETWORK_STATE`、`READ_MEDIA_IMAGES`；只有 `MainActivity` 一个可启动 Activity。 |
| 2026-08-08 | APK 签名与对齐检查 | `apksigner verify`：v1/v2 通过、单签名者；`zipalign -c -P 4 -v 4` 通过。SHA-256：`4f5c2f21aaead6ec0527830aabac3483abaf9caab90e7cdf64a4bac41b70712a`。 |
| 2026-08-08 | 调试污染回归检查 | 最终 APK 无 `Reflections`、LeakCanary 启动入口，主清单图标未被 `ic_launcher_xmas` 覆写；交付变体 `debuggable=false`，应用启动不再配置 `StrictMode.penaltyDialog()`。 |
| 2026-08-08 | 全量单测基线 | 45 项中 44 项通过；`LrcUtilsTest.testTemplateLrcTranslationType1` 在 `LrcUtilsTest.kt:396` 失败。歌词入口已关闭且本次未改解析器，故不将全量单测写为全绿。 |
| 2026-08-08 | ADB 状态 | `adb devices` 未发现已连接设备；以下真机项目均未执行。 |
| 2026-08-08 | 用户旧版反馈 | 旧版已确认 MP3、MP4 音频和锁屏持续出声可用；不等价于 1.3.3 的视频画面、后台和启动回归。 |

## 未闭环验收

1. 红米 K80 Pro 直接覆盖安装 1.3.3：只剩一个“本地听歌”图标、图标回到主应用图标、首次启动不出现 StrictMode 弹窗。
2. 在目标机验证 MP3、MP4+A​​AC 视频画面、锁屏 60 秒、通知、蓝牙、飞行模式和“行为设置”“诊断与日志”。
3. 队列中间项在任意进度点击上一首均切换上一项；第一项才回到 `0:00`；确认六档倍速与默认未锁定状态。
4. 处理既有 `LrcUtilsTest` 失败，并在不恢复歌词入口的前提下复跑全量单元测试。
5. 持有者提供 release keystore 后，验证正式签名、覆盖更新路径与商店发布链路。

## 验收判定规则

构建、静态检查和 APK 解析只能证明产物结构正确；视频硬件解码、HyperOS 后台、锁屏和蓝牙必须以目标机实测为准。任何未运行项目均标记为“未执行”，不能写为“通过”。
