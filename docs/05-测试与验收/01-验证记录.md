# 验证记录

> 状态：SelfBuilt Debug APK 已构建并完成包检查；Release 构建和红米 K80 Pro 新版真机回归未通过或未执行
> 更新时间：2026-08-08

## 目录

- [当前证据](#当前证据)
- [未闭环验收](#未闭环验收)
- [验收判定规则](#验收判定规则)

## 当前证据

| 日期 | 检查 | 结果 |
|---|---|---|
| 2026-08-08 | `:app:compileDebugKotlin` | 通过；新增视频渲染、诊断中心、设置修复可编译 |
| 2026-08-08 | 定向单元测试 | 通过：`Mp4AacSupportTest`、`DiagnosticStoreTest`、`BehaviorSettingsActivityTest` 共 7 个测试方法 |
| 2026-08-08 | `:app:testDebugUnitTest` | 45 个测试执行，44 个通过；既有 `LrcUtilsTest.testTemplateLrcTranslationType1` 断言失败。本次未改歌词解析器，不能把全量单测称为全绿。 |
| 2026-08-08 | `:app:assembleDebug` | 通过；生成可安装 APK：`本地听歌-1.3.1.c5886f9-debug.apk`，`versionCode 25`。 |
| 2026-08-08 | `:app:lintDebug` | 通过：0 个错误、122 个非阻断警告；修复 3 个 Media3 返回码、1 个实验 API 声明、5 个翻译格式错误。 |
| 2026-08-08 | 1.3.2 定向单元测试 | 通过：`PlaybackSpeedPresetTest`、`Mp4AacSupportTest`、`DiagnosticStoreTest`、`BehaviorSettingsActivityTest` 共 8 个测试方法；速度预设固定为 0.75x、1x、1.3x、1.5x、1.7x、2x。 |
| 2026-08-08 | 1.3.2 `:app:lintDebug` | 通过：0 个错误、122 个非阻断警告。 |
| 2026-08-08 | APK 清单检查 | 通过：`targetSdk 36`；含 `READ_MEDIA_AUDIO`、`READ_MEDIA_VIDEO`，不含 `INTERNET`、`ACCESS_NETWORK_STATE`、`READ_MEDIA_IMAGES`。 |
| 2026-08-08 | `:app:assembleNonMinifiedRelease` | 失败且连续复现：`misc:alacdecoder`、`media3:lib-decoder-midi` 的 Release Java 编译无法解析 `androidx.media3.exoplayer.audio` 等上游类。此问题阻断正式 Release APK，不以 Debug 产物替代。 |
| 2026-08-08 | 源码检查 | 旧 `BugHandlerActivity`、强制邮件调用和 `logcat -dball` 调用链已移除 |
| 2026-08-08 | 用户目标机反馈 | 旧版已确认 MP3、MP4 音频和锁屏持续出声可用；不等价于新版视频画面回归 |
| 2026-08-06 | `ffprobe` 本地样本 | 样本包含 HEVC 视频轨与 AAC-LC 48 kHz 双声道音轨 |

## 未闭环验收

1. 修复或隔离上游 Media3 Release 变体依赖，再执行 `:app:assembleNonMinifiedRelease`、正式签名、zipalign 和 SHA-256 校验。
2. 处理既有 `LrcUtilsTest` 失败，并在不恢复歌词入口的前提下复跑全量单元测试。
3. 评估 122 个非阻断 Lint 警告，按影响面拆分处理；不得以新增 baseline 隐藏问题。
4. 红米 K80 Pro：安装 1.3.2 后验证扫描、MP4 画面、MP3/MP4 锁屏 60 秒、通知、蓝牙、飞行模式、“行为设置”和“诊断与日志”。队列中间项在 2 秒和 11 秒点击上一首均应切换上一项；第一项才回到 `0:00`；确认六档倍速与默认未锁定状态。

## 验收判定规则

构建或单元测试通过只能证明代码可编译或规则正确；视频硬件解码、HyperOS 后台、锁屏和蓝牙必须以目标机实测为准。任何未运行项目都写为“未执行”，不能写为“通过”。
