# 验证记录

状态：1.6.2 交付；更新时间：2026-08-13。

## 自动化与静态证据

| 检查 | 命令或工具 | 结果 |
| --- | --- | --- |
| 视频续播定向测试 | `:app:testLocalDebugUnitTest --tests VideoResumeProgressStoreTest` | 4/4 通过：相同指纹恢复、文件修改后失效、播完清除、音频不建档、重新创建仓库后 A/B 两条记录独立恢复。 |
| 课程变体定向测试 | `:app:testCourseDebugUnitTest --tests VideoResumeProgressStoreTest` | 4/4 通过。 |
| 并发回归测试 | 双变体 `PauseableFlowsTest` | 首轮发现测试以真实毫秒延迟断言具体序列，调度波动会将暂停窗口边界从 3/6 改为 2/5；已改为受控信号验证“暂停时只保留最新值、恢复后准确交付”，双变体通过。 |
| 全量双变体单元测试 | `:app:testLocalDebugUnitTest :app:testCourseDebugUnitTest` | 两个变体各 56 项、共 112 项通过，0 失败。 |
| 双变体构建 | `:app:assembleLocalDebug :app:assembleCourseDebug` | 通过，分别生成可安装 APK。 |
| APK 结构 | `aapt2` | “本地听歌”：`org.akanework.gramophone.debug`；“多听课程”：`org.akanework.gramophone.course.debug`；均为 versionCode 34、versionName `1.6.2`、targetSdk 36。 |
| 离线边界 | 合并清单与 APK 清单检查 | 两个变体均不含 `INTERNET`、`ACCESS_NETWORK_STATE`。 |
| 签名与对齐 | `apksigner`、`zipalign` | 两包 v1/v2 签名有效、使用同一 SelfBuilt 证书，ZIP 对齐通过。 |
| 课程品牌资源 | `aapt2 dump resources` | 课程包的 `ic_launcher` 指向 course 覆写资源；含白底居中的 `course_launcher_mark`、通知小图标 `ic_gramophone_monochrome` 与固定火焰橙主色。 |

## 红米 K80 Pro 真机边界

本轮执行 `adb devices -l` 时没有可用设备，因此未安装到设备，也不把本地构建当作真机验收。请在 Redmi 24122RKC7C 上按《产品范围与验收》的步骤，重点确认：两个图标并存、视频 A/B 互不覆盖进度、全屏进退、锁屏后台、系统回收后的最大 15 秒进度丢失边界。

## 交付校验值

| APK | SHA-256 |
| --- | --- |
| `outputs/本地听歌-1.6.2.apk` | `ad1757687bfd9cb76e3526003807691c2233fadd4df45c913bcd9c527ee150ac` |
| `outputs/多听课程-1.6.2.apk` | `ba74dc1e9f77592555fa456792e260b451e94bfa4444e5653430e2b5c4c80d75` |

签名为 SelfBuilt，不等同于正式商店签名；未提供正式 keystore 前，不能证明未来商店包与当前 APK 的升级兼容性。
