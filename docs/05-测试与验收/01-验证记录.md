# 验证记录

状态：1.6.3 交付候选；更新时间：2026-08-15。

## 自动化与静态证据

| 检查 | 命令或工具 | 结果 |
| --- | --- | --- |
| 视频续播定向测试 | `:app:testLocalDebugUnitTest --tests VideoResumeProgressStoreTest` | 7/7 通过：A/B 独立、文件修改后失效、播完清除、音频不建档、重建存储后恢复；另覆盖“扫描对象带路径、播放器/队列对象只有 MediaStore ID”的双向续播，以及播放器缺失修改时间时的恢复。 |
| 课程变体定向测试 | `:app:testCourseDebugUnitTest --tests VideoResumeProgressStoreTest` | 7/7 通过。 |
| 并发回归测试 | 双变体 `PauseableFlowsTest` | 首轮发现测试以真实毫秒延迟断言具体序列，调度波动会将暂停窗口边界从 3/6 改为 2/5；已改为受控信号验证“暂停时只保留最新值、恢复后准确交付”，双变体通过。 |
| 全量双变体单元测试 | `:app:testLocalDebugUnitTest :app:testCourseDebugUnitTest` | 两个变体各 56 项、共 112 项通过，0 失败。 |
| 双变体构建 | `:app:assembleLocalDebug :app:assembleCourseDebug` | 通过，分别生成可安装 APK。 |
| APK 结构 | `aapt2 dump badging` | “本地听歌”：`org.akanework.gramophone.debug`；“多听课程”：`org.akanework.gramophone.course.debug`；均为 versionCode 35、versionName `1.6.3`、targetSdk 36。 |
| 离线边界 | APK 清单检查 | 两个 APK 均不含 `INTERNET`、`ACCESS_NETWORK_STATE`。 |
| ZIP 完整性 | `zipinfo -t` | 两个 APK 均通过 ZIP 目录完整性检查。 |
| 课程品牌资源 | `aapt2 dump resources` | 课程包的 `ic_launcher` 指向 course 覆写资源；含白底居中的 `course_launcher_mark`、通知小图标 `ic_gramophone_monochrome` 与固定火焰橙主色。 |

## 红米 K80 Pro 真机边界

本轮执行 `adb devices -l` 时没有可用设备，因此未安装到设备，也不把本地构建当作真机验收。请在 Redmi 24122RKC7C 上按《产品范围与验收》的步骤，重点确认：两个图标并存、视频 A/B 互不覆盖进度、全屏进退、锁屏后台、系统回收后的最大 15 秒进度丢失边界。

## 交付校验值

| APK | SHA-256 |
| --- | --- |
| `outputs/本地听歌-1.6.3.apk` | `76af755aba7e0e889ccaeeff80afe04be2a8d75fec91336062ca31a22682a7c2` |
| `outputs/多听课程-1.6.3.apk` | `53bfb80d697701e998cdd7cc2091e134a09159d9e5d4032d75f586e7782b9573` |

两包由 `assemble*Debug` 的项目签名任务生成，且 `isDebuggable=false`；本机未找到独立的 `apksigner`/`zipalign` CLI，因此本轮不将 v1/v2 签名与 zipalign 结论伪称为已复核。签名仍为 SelfBuilt，不等同于正式商店签名；未提供正式 keystore 前，不能证明未来商店包与当前 APK 的升级兼容性。
