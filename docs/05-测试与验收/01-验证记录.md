# 验证记录

状态：1.5.0 最终交付；更新时间：2026-08-12。

## 自动化与静态证据

| 检查 | 命令或工具 | 结果 |
| --- | --- | --- |
| 定向回归 | `:app:testDebugUnitTest --tests LocalLibraryManagerTest --tests LibraryPresentationTest` | 3/3 通过：所选恢复不影响其他隐藏项；标题三行、16sp 且副标题锚点隐藏；单曲菜单无 `delete`、含私有移除项。 |
| 全量单元测试 | `:app:testDebugUnitTest` | 52 项中 51 项通过；唯一失败为既有隐藏歌词解析 `LrcUtilsTest.testTemplateLrcTranslationType1`，不在本次媒体库或播放器改动链路。 |
| 修复后构建 | `:app:testDebugUnitTest --tests LibraryPresentationTest :app:assembleDebug` | 通过；用于产出最终 APK。 |
| 静态检查 | `:app:lintDebug` | 0 Error、129 Warning。警告主要来自既有多语言资源、上游 API 与兼容性提示；不以 baseline 隐藏。 |
| APK 结构 | `aapt2`、`apksigner`、`zipalign` | 包名 `org.akanework.gramophone.debug`、versionCode 31、versionName `1.5.0`、targetSdk 36；唯一 `MainActivity` 启动入口；v1/v2 签名、zipalign 通过；无 `INTERNET` 与 `ACCESS_NETWORK_STATE`。 |

## 红米 K80 Pro 真机证据

设备：Redmi 24122RKC7C；Android 16 / SDK 36；通过 USB ADB 连接。

| 场景 | 动作 | 结果 |
| --- | --- | --- |
| 覆盖安装 | `adb install -r outputs/本地听歌-1.5.0.apk` | 成功。 |
| 冷启动与加载媒体列表 | 最终 APK 覆盖安装、强制停止后启动 `MainActivity` | 成功；启动耗时 317ms；进程存活、`MainActivity` 为前台。原先“移除副标题视图”导致的 `BaseAdapter.kt:364` 空指针已在首轮真机发现并修复。 |
| 首页三行标题 | 读取 UIAutomator 视图树 | 成功；长标题完整进入标题控件，列表未显示“未知音乐人”副标题。 |
| StrictMode 循环弹窗 | 清空 logcat 后冷启动并检查应用进程日志/界面树 | 未出现本应用 `StrictMode` 违规弹窗、`FATAL EXCEPTION` 或应用自身 `NullPointerException`。系统其他进程的日志不计入此结果。 |
| 启动器入口 | `cmd package resolve-activity` | 仅解析到 `org.akanework.gramophone.ui.MainActivity`。 |
| 离线权限 | `dumpsys package` 与 APK 清单检查 | 未发现 `INTERNET`、`ACCESS_NETWORK_STATE`。 |
| 行为设置页 | 通过 ADB 直接启动 | Android 16 正确拒绝 Shell 启动未导出的 Activity；设备策略同时拒绝 `adb input` 注入点击。因此本轮不能伪称完成真实点击回归，需用户在应用内点“设置 → 行为设置”复验。源码中已保持 `album_covers` 节点与声明式隐藏逻辑，避免原先的空指针。 |

## 仍需人工确认的媒体体验

本轮不能绕过设备的输入注入限制，也不应自动操作或隐藏用户真实媒体，因此以下留给人工验收：MP3/MP4 实际播放、视频竖横屏连续出帧、锁屏/蓝牙控制、逐项移出与恢复、分类勾选。测试时可从“媒体库整理”进行移出；该动作仅写应用私有记录，提示为“已移出媒体库；手机文件未删除”。

APK：`outputs/本地听歌-1.5.0.apk`；SHA-256：`f28f8568b88fd1e4194a7e9c165755d6bb9bb18006a49ecca47dcbd2a0be9ed1`。
