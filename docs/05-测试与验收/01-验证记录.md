# 验证记录

状态：1.7.3 原生视觉交付候选；更新时间：2026-08-22。

## 1.7.3 原生视觉与双 APK 复验（2026-08-22）

| 检查 | 命令或证据 | 结果 |
| --- | --- | --- |
| 双变体单元测试 | `:app:testLocalDebugUnitTest :app:testCourseDebugUnitTest` | 本地听歌 61 项、多听课程 61 项，均 0 失败、0 错误。 |
| 双变体 Lint | `:app:lintLocalNonMinifiedRelease :app:lintCourseNonMinifiedRelease` | 本地听歌 0 error / 133 warning；多听课程 0 error / 140 warning。告警为既有 SDK、注释与兼容性提示。 |
| 双变体构建 | `:app:assembleLocalNonMinifiedRelease :app:assembleCourseNonMinifiedRelease` | `BUILD SUCCESSFUL`；两个 APK 均生成。 |
| APK 元数据 | `aapt2 dump badging` | 本地听歌包名 `org.akanework.gramophone`；多听课程包名 `org.akanework.gramophone.course`；均 versionCode 38、versionName `1.7.3`、targetSdk 36。 |
| 离线权限 | `aapt2 dump permissions` | 两个 APK 均不含 `INTERNET`、`ACCESS_NETWORK_STATE`。 |
| ZIP 完整性 | `unzip -tq` | 两个 APK 均通过。 |
| 原生品牌与布局 | Compose 主题、`colors.xml`、课程 logo 资源、列表 XML 静态检查 | 本地听歌 `#FDB833`、多听课程 `#FF4E00`；课程 logo 白底居中缩小；主标题最多三行，副标题隐藏。 |

本节为当前 1.7.3 证据；下方 1.7.2 内容保留为历史记录，不代表当前 APK 元数据。

## 自动化与静态证据

| 检查 | 命令或工具 | 结果 |
| --- | --- | --- |
| 视频续播定向测试 | `:app:testLocalDebugUnitTest --tests VideoResumeProgressStoreTest` | 7/7 通过：A/B 独立、文件修改后失效、播完清除、音频不建档、重建存储后恢复；另覆盖“扫描对象带路径、播放器/队列对象只有 MediaStore ID”的双向续播，以及播放器缺失修改时间时的恢复。 |
| 课程变体定向测试 | `:app:testCourseDebugUnitTest --tests VideoResumeProgressStoreTest` | 7/7 通过。 |
| 外放强音定向测试 | 双变体 `ReplayGainAudioProcessorTest` | 各 2/2 通过：开启时强制走可压缩的 Float PCM 音频链路；关闭后恢复原 PCM 格式。 |
| 代码审计修复回归 | `DiagnosticStoreTest`、媒体扫描静态检查、MediaSession 参数校验编译 | 诊断事件异步写盘、崩溃事件同步落盘、诊断页 I/O 脱离主线程、音频 DATA 空值防御、自定义命令可信边界和启动日志脱敏均已纳入 1.7.2 源码。 |
| 并发回归测试 | 双变体 `PauseableFlowsTest` | 首轮发现测试以真实毫秒延迟断言具体序列，调度波动会将暂停窗口边界从 3/6 改为 2/5；已改为受控信号验证“暂停时只保留最新值、恢复后准确交付”，双变体通过。 |
| 全量双变体单元测试 | `:app:testLocalDebugUnitTest :app:testCourseDebugUnitTest` | 每个变体 61 项、共 122 项通过，0 失败、0 错误；覆盖续播、队列、诊断、MP4+A​​AC、播放页、设置与外放强音处理器。 |
| 双变体 Lint 与构建 | `:app:lintLocalDebug :app:lintCourseDebug :app:assembleLocalDebug :app:assembleCourseDebug` | 构建通过；Lint：本地听歌 0 error / 131 warning，课程包 0 error / 138 warning。警告均为既有 SDK、依赖、歌词注释或 Android 14 部分媒体访问提示，本轮未新增 Lint error。 |
| APK 结构 | `aapt2 dump badging` | “本地听歌”：`org.akanework.gramophone.debug`；“多听课程”：`org.akanework.gramophone.course.debug`；均为 versionCode 37、versionName `1.7.2`、targetSdk 36。 |
| 离线边界 | APK 清单检查 | 两个 APK 均不含 `INTERNET`、`ACCESS_NETWORK_STATE`。 |
| ZIP 完整性 | `zipinfo -t` | 两个 APK 均通过 ZIP 目录完整性检查。 |
| 课程品牌资源 | `aapt2 dump resources` | 课程包的 `ic_launcher` 指向 course 覆写资源；含白底居中的 `course_launcher_mark`、通知小图标 `ic_gramophone_monochrome` 与固定火焰橙主色。 |

## 红米 K80 Pro 真机边界

本轮执行 `adb devices -l` 时没有可用设备，因此未安装到设备，也不把本地构建当作真机验收。请在 Redmi 24122RKC7C 上按《产品范围与验收》的步骤，重点确认：两个图标并存、视频 A/B 互不覆盖进度、全屏进退、锁屏后台、系统回收后的最大 15 秒进度丢失边界。

## 交付校验值

| APK | SHA-256 |
| --- | --- |
| `outputs/本地听歌-1.7.2.apk` | `a1ba892d3e3287ffcd4d27f20e6a5f6e5335b73b9c89a0acf80a6504b8726bb3` |
| `outputs/多听课程-1.7.2.apk` | `845336df3648652650c30d0cab8c5bfd6cee1cf1c5c622157dfc1804dce7a330` |

两包由 `assemble*Debug` 的项目签名任务生成，且 `isDebuggable=false`；已通过 `aapt2` 包元数据、权限清单与构建任务验证。本机未找到独立的 `apksigner`/`zipalign` CLI，因此本轮不将 v1/v2 签名与 zipalign 结论伪称为已复核。签名仍为 SelfBuilt，不等同于正式商店签名；未提供正式 keystore 前，不能证明未来商店包与当前 APK 的升级兼容性。
