# 审计复核与后续任务

状态：1.4.2；更新时间：2026-08-10。

外部审计提出的 StrictMode 循环弹窗、三个启动器、圣诞图标、设置页空指针、诊断导出体验和 Debug 签名问题已逐项复核：前五项分别通过移除 `penaltyDialog`、调试启动器/LeakCanary、debug Manifest 覆写、孤儿 Preference 引用和增加一键复制处理。签名问题不能靠代码伪造解决，仍需正式 keystore。

本轮静态安全复核范围：清单无 `INTERNET`；诊断使用应用私有目录；一键复制事件摘要会脱敏路径和 `content://` URI；FileProvider 仅用于用户主动 ZIP 导出；不扫描或上报用户媒体。待处理风险：第三方依赖 CVE 需结合联网依赖扫描；原生解码器和大 MP4 曲库需要运行时测试；所有真机结论待红米回归。
