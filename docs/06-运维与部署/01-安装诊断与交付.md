# 安装诊断与交付

状态：1.4.0；更新时间：2026-08-10。

安装包：`outputs/本地听歌-1.4.0.apk`，SHA-256 为 `b160de4f4ae1d155ff09f948d641a713b99166ffa01d3655d8d7d580dce11766`。安装前关闭旧进程，直接覆盖同包名的旧 SelfBuilt 版本；如系统提示签名不一致，先导出诊断包和当前版本信息，再卸载旧包后安装。卸载会清除应用私有的隐藏媒体与分类数据，但不会删除手机音视频文件。

异常处理顺序：设置 → 诊断与日志 → 一键复制诊断摘要；只有需要完整历史时再导出 ZIP。若视频无画面，先确认文件是 MP4 + AAC，并把诊断摘要与复现步骤一并提供。
